spring:
  jpa:
    properties:
      hibernate:
        format_sql: true
  #        use_sql_comments: true

  h2:
    console:
      enabled: true

  output:
    ansi:
      enabled: always

logging:
  level:
    org:
      hibernate:
        SQL: debug
      springframework:
        transaction: debug
        orm:
          jpa: debug
#        web: info

decorator:
  datasource:
    p6spy:
      enable-logging: false